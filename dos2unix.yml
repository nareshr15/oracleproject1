---
  - name: install doc2unix eeeeeee
    hosts: localhost
    vars:
      http_port: 80
    remote_user: satishubuntu

    tasks:
    - name: doc2unix installation task
      dpkg:
        name: dos2unix
        state: latest
