---
  - name: install doc2unix eeeeeee
    hosts: agent123
    vars:
      http_port: 80
    remote_user: root

    tasks:
    - name: doc2unix installation task
      yum:
        name: dos2unix
        state: latest
