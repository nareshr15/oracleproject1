---
  - name: install doc2unix eeeeeee
    hosts: localhost
    vars:
      http_port: 80
    remote_user: root

    tasks:
    - name: doc2unix installation task
      apt:
        name: dos2unix
        state: latest
